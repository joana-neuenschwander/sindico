<div class="container">
  <% @places.each do |place| %>
    <%= place.name %>
    <%= place.description %>
    <%= link_to "#{place.name}", place_path(place) %>
  <% end %>
<%= link_to "Novo", new_place_path %>
</div>
